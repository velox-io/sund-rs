[package]
name = "sund-json"
description = "Suspendable JSON parser with SIMD structural scanning and reactor-style output."
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
