[package]
name = "sund-core"
description = "Zero-allocation, suspendable JSON parsing kernel with SIMD structural scanning and reactor-style output."
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]

[build-dependencies]
cc = "1"
